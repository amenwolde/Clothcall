package com.clothcall.api

// Kept as an alias so any stale import still resolves while the codebase migrates.
@Deprecated("Use GeminiApiService directly", replaceWith = ReplaceWith("GeminiApiService"))
typealias ClaudeApiService = GeminiApiService
